# ledger

This directory is a deterministic contract surface placeholder.

Public interface contract for this subsystem MUST be defined and completed according to the authoritative specifications in `/docs/`.
